eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/
# 自定义客户端跳转地址
#  instance:
#    hostname: clientName
spring:
  application:
    name: client-one
server:
  port: 9081