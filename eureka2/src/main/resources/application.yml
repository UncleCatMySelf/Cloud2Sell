eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false
# 关闭自我保护模式
  server:
    enable-self-preservation: false
spring:
  application:
    name: eureka
server:
  port: 8762